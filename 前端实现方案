基于你提供的架构文档，下面梳理出**悬浮面板**和**Popup管理弹窗**中每一个控件的功能服务逻辑与具体实现方式，可直接用于指导代码编写。

---

## 一、悬浮面板（Content Script 注入）控件逻辑

悬浮面板由 `src/ui/panel.js` 构建，所有事件绑定在 `bindPanelEvents()` 中完成。

### 1.1 Header 区域

| 控件 | 功能 | 服务逻辑 |
|------|------|----------|
| **最小化按钮 `−`** | 收起面板，显示悬浮小球 | `click` → 调用 `togglePanel(false)` 隐藏面板，同时显示 `#ds-pet-ball`。`togglePanel` 通过 `classList.remove('visible')` 和 `style.display='none'` 双重保证隐藏。 |
| **关闭按钮 `×`** | 关闭面板（行为同最小化） | 与 `−` 绑定相同逻辑，最终仍可点击小球重新打开。 |

### 1.2 左栏：工具与技能

| 控件 | 功能 | 服务逻辑 |
|------|------|----------|
| **搜索输入框** | 实时过滤工具卡片 | `input` 事件 → 200ms 防抖 → 调用 `renderToolsList(tools)`，内部用 `filter` 匹配工具名/描述，重新渲染卡片列表。 |
| **工具卡片上的模式指示器** | 循环切换工具模式 | 点击卡片上的模式区域 → 获取当前工具ID → 循环模式枚举 `AUTO → MANUAL → OFF → AUTO` → 调用 `window.__updateToolMode(toolId, newMode)`（内部通过 `actions.js` 发送消息给 background 更新配置，然后同步到 storage）。视觉上更新卡片样式：AUTO 显示闪电符号、MANUAL 圆点、OFF 置灰。 |
| **技能开关胶囊** | 启用/禁用技能 | 点击胶囊 → 切换该技能的 `enabled` 布尔值 → 调用 `window.__toggleSkill(skillId)` → 内部通过 `actions.js` 发送更新请求，并同步到 `chrome.storage.sync`。UI 实时切换 `toggle on/off` 类。 |
| **`+ Add Skill` 按钮** | 弹出添加技能弹窗 | `click` → 调用 `showModal('Add Skill', ...)` 显示输入框（名称、描述、指令），点击 Save 时推入技能数组并保存到 storage。 |

### 1.3 右栏：实时日志

| 控件 | 功能 | 服务逻辑 |
|------|------|----------|
| **日志级别 Tab (All/Info/Warn/Error)** | 按级别过滤日志显示 | 点击任一 Tab → 设置 `currentLogLevel` 变量 → 调用 `renderLogs(logs)`，过滤出对应级别条目重新渲染。 |
| **`Export` 按钮** | 导出当前日志为文件 | `click` → 调用 `window.__exportLogs()` → 内部使用 `Blob` 生成文本内容，创建临时 `<a>` 元素触发下载，文件名 `agent-logs-{timestamp}.txt`。 |
| **`Clear` 按钮** | 清空日志 | `click` → 弹出确认对话框（可选）→ 调用 `window.__clearLogs()` → 内部发送消息给 background 清除后端日志列表，同时清空前端 `logs` 数组并重新渲染。 |
| **自动滚动切换按钮 `⏬`** | 暂停/恢复自动滚动到最新日志 | `click` → 切换布尔变量 `autoScroll` → 在 `renderLogs` 后若 `autoScroll` 为真，执行 `logArea.scrollTop = logArea.scrollHeight`。按钮图标/样式随状态变化。 |

### 1.4 底部固定操作栏

| 控件 | 功能 | 服务逻辑 |
|------|------|----------|
| **快捷操作按钮（如“总结会话”）** | 一键发送预设指令 | `click` → 根据按钮索引从 `config.quickActions` 取出 `command` → 调用 `window.__triggerQuickAction(idx)` → 内部通过 `injected.js` 的 `setInputValue` 将指令文本填入聊天输入框，并模拟点击发送按钮。 |
| **编辑按钮 `✎`** | 打开快捷操作编辑器 | `click` → 显示 QA Editor 弹窗（模态框），内部动态生成 Label + Prompt 输入组（最多5个），点击 Save 时更新 `config.quickActions` 并保存到 storage。 |

### 1.5 悬浮小球 (Pet Ball)

| 控件 | 功能 | 服务逻辑 |
|------|------|----------|
| **小球点击** | 展开/收起面板 | `mousedown` 记录坐标，`mouseup` 计算位移 < 5px 则判定为点击，调用 `togglePanel()`。 |
| **小球拖拽** | 移动小球位置 | `mousedown` → `mousemove` 更新 `left/top` → `mouseup` 解除监听。拖拽过程中面板不触发打开。 |

### 1.6 未初始化引导状态

| 控件 | 功能 | 服务逻辑 |
|------|------|----------|
| **`[>] Initialize Agent` 按钮** | 初始化 Agent 并连接后端 | `click` → 调用 `window.__initAgent()` → 内部通过 `actions.initAgent()` 发送初始化请求（包含人格/记忆/系统提示）到后端，成功后在 storage 标记 `initialized=true`，面板切换为完整界面。 |

---

## 二、Popup 管理弹窗（插件设置界面）控件逻辑

全部交互在 `popup.js` 中，采用 300ms 防抖自动保存至 `chrome.storage.sync`，键名为 `ds_agent_config`。

### 2.1 公共机制

- **防抖自动保存**：所有输入/下拉/开关 `change` 事件 → 300ms 防抖 → `chrome.storage.sync.set({ ds_agent_config: config })`。
- **Toast 提示**：`showToast(msg)` 在底部居中显示 2 秒后自动消失。
- **确认对话框**：危险操作（删除、清空、重置）弹出 `confirm('确定要XXX？')`，部分需输入 `DELETE` 二次验证。

### 2.2 各页签控件明细

#### 2.2.1 Connection 页

| 控件 | 功能 | 实现 |
|------|------|------|
| **后端地址输入框** | 设置 API 基础 URL | `input` 防抖保存 `config.apiBaseUrl`。保存前可做 URL 格式校验。 |
| **API Key 输入框** | 设置密钥 | 类型为 `password`，旁设 `[Show]` 文字按钮切换 `type` 属性。防抖保存 `config.apiKey`，自动去除首尾空格。 |
| **超时输入框** | 请求超时秒数 | 限制数字 1-30，防抖保存 `config.timeout`，超出范围恢复原值。 |
| **WebSocket 地址输入框** | 可选实时推送地址 | 防抖保存 `config.wsUrl`，允许为空。 |
| **状态指示（绿/红点）** | 显示连接状态 | 读取 `config.connectionStatus`，由 background 定时检测并更新到 storage。 |
| **`[Test Connection]` 按钮** | 测试后端连通性 | `click` → 读取当前输入的 URL 和 Key → 通过 `chrome.runtime.sendMessage({ action: 'testConnection', payload: { url, key } })` → background 发起 `fetch(url + '/ping')`，结果返回后 popup 更新状态指示并 Toast。成功时自动保存当前 URL 和 Key。 |

#### 2.2.2 Tools 页

| 控件 | 功能 | 实现 |
|------|------|------|
| **模式下拉框（每行）** | 切换工具默认模式 | `change` → 更新对应工具对象的 `defaultMode`，保存整个 `tools` 数组。 |
| **`[✎ Edit]` 按钮** | 编辑工具参数 | `click` → 调用 `showModal('Edit Tool', ...)`，预填当前值，保存后更新数组并回写 storage。 |
| **`[✕ Delete]` 按钮** | 删除工具 | 确认后从 `tools` 数组中移除该项，保存。 |
| **`[+ Add Tool]` 按钮** | 添加自定义工具 | 弹出空白模态框（名称、描述、指令模板、模式），保存时推入数组。 |

#### 2.2.3 Skills 页

| 控件 | 功能 | 实现 |
|------|------|------|
| **技能开关（每行）** | 启用/禁用技能 | `click` → 切换 `enabled` 布尔值，保存整个 `skills` 数组。视觉显示 “✓ 开启”或“✗ 关闭”。 |
| **`[✕ Delete]` 按钮** | 删除技能 | 确认后从 `skills` 中移除，保存。 |
| **`[+ Add Skill]` 按钮** | 添加新技能 | 弹出模态框（名称、描述、指令模板、默认启用状态），保存时推入数组。 |

#### 2.2.4 Quick Actions 页

| 控件 | 功能 | 实现 |
|------|------|------|
| **Label + Command 输入组** | 编辑快捷操作 | 直接修改输入框值，防抖保存整个 `quickActions` 数组。 |
| **`[+ Add]` 按钮** | 添加一行空白 | 推入 `{label:'', command:''}`，限制最多5个，自动聚焦新标签框。 |
| **`[Import Preset]` 按钮** | 导入内置预设 | `click` → 显示预设列表（如“总结会话”等5个），选择后覆盖或追加到当前列表，保存。 |
| **`[✕ Remove]` 按钮（每行）** | 删除该快捷项 | 从数组中 `splice`，保存。 |

#### 2.2.5 Logs 页

| 控件 | 功能 | 实现 |
|------|------|------|
| **级别 Tab** | 设定悬浮面板日志过滤等级 | `click` → 更新 `config.logLevel`（`all`/`info`/`warn`/`error`），保存。 |
| **Max entries 输入框** | 限制日志保留条数 | 范围 50-2000，防抖保存 `config.maxLogEntries`。 |
| **Notification Toggle** | 错误时桌面通知开关 | 切换 `config.desktopNotification`，首次开启需请求 `notifications` 权限。 |
| **`[Export Now]` 按钮** | 导出当前日志文件 | `click` → 通过 `sendMessage` 向 background 请求全量日志 → 生成 Blob 下载。 |
| **`[Clear Logs]` 按钮** | 清空所有日志 | 确认后发送 `{ action: 'clearLogs' }` 给 background，清空后同步所有面板。 |
| **日志预览区** | 显示最近20条日志 | 直接从 background 获取最近日志并渲染，只读。 |

#### 2.2.6 Appearance 页

| 控件 | 功能 | 实现 |
|------|------|------|
| **Theme 切换** | 亮色/暗色/跟随系统 | `change` → 保存 `config.theme`，悬浮面板实时切换 CSS 变量。 |
| **Panel Position 下拉** | 面板默认角落 | 保存 `config.panelPosition`，可选值：`bottom-right` 等。悬浮面板读取后更新 fixed 定位坐标。 |
| **Draggable Toggle** | 是否允许拖拽面板/小球 | 保存 `config.draggable`。悬浮面板根据该值绑定/解绑拖拽事件。 |
| **Font Size Slider** | 面板字体大小 | 范围 12-18px，保存 `config.fontSize`，悬浮面板动态设置 `document.documentElement.style.fontSize`。 |
| **实时预览小窗** | 预览外观 | 根据当前配置渲染一个微缩面板样式图，使用相同 CSS 变量即可自动同步。 |

#### 2.2.7 Data 页

| 控件 | 功能 | 实现 |
|------|------|------|
| **`[Export Config]`** | 导出全部配置为 JSON | 读取 `ds_agent_config` → 生成 Blob → 触发下载。 |
| **`[Import Config]`** | 合并导入 JSON 配置 | 创建隐藏 `<input type="file">`，读取文件、解析 JSON、深度合并到现有配置，保存。 |
| **`[Reset Defaults]`** | 恢复出厂配置 | 确认后，将配置重置为硬编码默认值并写入 storage，同时通知 background 清空日志。 |
| **`[Clear All]`** | 删除所有数据 | 弹出二次确认（需输入“DELETE”），确认后调用 `chrome.storage.sync.clear()` 或移除指定 key，并重置默认配置。 |

#### 2.2.8 About 页

| 控件 | 功能 | 实现 |
|------|------|------|
| **`[Run Diagnostics]`** | 收集诊断信息并复制 | 收集版本号、连接状态、工具/技能数、最后错误等信息，拼接文本后调用 `navigator.clipboard.writeText`，Toast 提示。 |
| **`[Open GitHub]`** | 打开项目链接 | `click` → `chrome.tabs.create({ url: 'https://github.com/...' })`。 |

---

## 三、通信与数据流实现要点

所有按钮逻辑最终都通过以下路径之一影响系统：

- **悬浮面板按钮** → 调用 `window.__xxx` 函数（定义在 `panel.js` 或 `actions.js`）→ 内部通过 `chrome.runtime.sendMessage` 或直接 `fetch` 与后端/background 交互，并更新 storage 和 UI。
- **Popup 按钮** → 直接操作 `chrome.storage.sync`，利用 `onChanged` 事件自动同步到悬浮面板和 background。
- **后端交互**：`actions.js` 中的 `initAgent`, `loadTools`, `executeTool` 等直接 `fetch` 本地 `localhost:3002` 的 HTTP API，或由 background 代理转发。
- **日志推送**：后端通过 WebSocket（可选）或 HTTP 响应返回日志，`background.js` 接收后通过 `chrome.tabs.sendMessage` 推送到 content script，再由 `panel.js` 渲染。

按照上述设计，你可以直接为每个控件编写代码逻辑，无需额外构思。